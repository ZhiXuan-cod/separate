# requirements.txt
# Core
streamlit>=1.32.0
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
plotly>=5.18.0

# Database
supabase>=2.0.0

# AutoML (optional but recommended — falls back to RandomForest without it)
# pycaret>=3.2.0

# Note: PyCaret has heavy dependencies (lightgbm, xgboost, etc.).
# Uncomment the line above and run:
#   pip install pycaret
# to enable full AutoML. The app works without it using scikit-learn fallback.
